RozgarRadar - Next.js starter (Deploy-ready)
Instructions:
1. Create a GitHub repo and upload this project's files (all folders).
2. Create a Vercel account and import the GitHub repo. Set environment variables from Firebase + OpenAI (if used).
3. Deploy. Admin panel at /admin (requires Firebase Auth user).
